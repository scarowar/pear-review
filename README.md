# Pear Review 🍐 — Fresh Slate

Day-0: archived prior work at `archive/2026-09-07-pre-reset/` (tag `archive/2026-09-07-pre-reset`). Fresh `main` has no connection to prior docs — see `VISION.md`.

Single goal: become one of the world's best Applied AI engineers by building Pear Review right.

We start with Wayfinder to name the vision. Every decision uses native selectors, not text dumps. Skills: `wayfinder` + `grilling` + `domain-modeling` + `show-me` + `eli5` installed globally for OpenCode.

See `AGENTS.md` for working rules.
